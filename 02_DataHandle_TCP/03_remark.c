#include <stdio.h>
/**
 * TCP原理
 * 
 * 服务端一次调用write函数，发送40字节数据；客户端如果缓慢分批接收10字节后，
 * 剩余的30字节在哪里呢？
 * 
 * write函数调用瞬间，数据移至输出缓冲；read函数调用瞬间，从输入缓冲读取数据。
 * 
 * TCP有滑动窗口协议，客户端这边的输入缓冲还能存储多少数据，是会向服务端报备的，
 * 因此TCP中不会因为缓冲溢出而丢失数据。
 * 
 * TCP内部工作原理1：与对方套接字的连接
 *    三次握手
 * 
 * TCP内部工作原理2：与对方主机的数据交换
 *    收发数据的默契约定
 * 
 * TCP内部工作原理3：断开与套接字的连接
 *    四次握手
*/

int main(){}